# 链上交互演示

## 交互流程

1. **部署合约** → 获得链上程序地址
2. **初始化** (`init_voting`) → 设置候选人初始票数
3. **投票** (`cast_vote`) → 链下生成加密 Ticket Record
4. **计票** (`tally_vote`) → 消费 Ticket，链上更新票数
5. **查询结果** → 通过 API 读取 Mapping 值

## 实际交互记录

<!-- 在完成链上交互后，粘贴命令行输出和截图 -->
